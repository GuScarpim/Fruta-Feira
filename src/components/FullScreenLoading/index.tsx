import { LoadingContainer, Spinner } from './styles'

const FullScreenLoading = () => {
  return (
    <LoadingContainer>
      <Spinner />
    </LoadingContainer>
  )
}

export default FullScreenLoading
